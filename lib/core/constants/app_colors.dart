import 'package:flutter/material.dart';

class AppColors {
  // Dark Theme Palette (Industrial/Premium)
  static const bg = Color(0xFF0A0A0A);
  static const surface = Color(0xFF141414);
  static const surfaceLight = Color(0xFF1E1E1E);
  static const accent = Color(0xFFE5FF00); // Neon Lime for "Premium" look
  
  static const textPrimary = Colors.white;
  static const textSecondary = Color(0xFFA0A0A0);
  static const textDim = Color(0xFF606060);
  
  static const border = Color(0xFF222222);
  static const borderLight = Color(0xFF333333);

  // Status Colors
  static const success = Color(0xFF00FF94);
  static const error = Color(0xFFFF4D4D);
  static const warning = Color(0xFFFFB800);

  // Expense Categories (Fixed colors for better UX)
  static const fuel = Color(0xFFFFD600);
  static const service = Color(0xFF00C2FF);
  static const wash = Color(0xFF00FF94);
  static const mods = Color(0xFFCC00FF);
  static const other = Color(0xFFFFA07A);
}
